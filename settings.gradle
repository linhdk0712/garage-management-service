pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            url = uri("https://repo1.maven.org/maven2/")
        }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        mavenCentral()
        maven {
            url = uri("https://repo1.maven.org/maven2/")
        }
        maven {
            url = uri("https://services.gradle.org/distributions/")
        }
        maven {
            url = uri("https://packages.adoptium.net/artifactory/adoptium/")
        }
        maven {
            url = uri("https://download.oracle.com/")
        }
        maven {
            url = uri("https://repo.bell-sw.com/java/")
        }
    }
}

rootProject.name = 'garage-management-service'
